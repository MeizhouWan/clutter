修正了某些极端情况可能会导致数据编辑器崩溃的问题。
修正了与数据表实例相关的内存泄漏。
修正了用经典地图转换器转换已经自定义了单位数据的地图时可能发生的崩溃。
修正了如果拥有单位不拥有该行为的层数时，行为累加器会被直接忽略，而实际上，这部分累加器应该返回0的问题。
GUI版本的触发动作，数据表中的值（单位引用）——实例(Value From Data Table (Unit Reference) – Instance)，现在能够正常工作了。
GUI版本的触发动作，数据表中值的名称——实例(Name Of Data Table Value – Instance)，现在能够正常工作了。
对增益行为持续时间覆盖(Duration Override)字段的变更现在可以保存了。
